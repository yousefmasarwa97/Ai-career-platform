Place the two app screenshots here with these exact filenames so the main
README renders them under the agent explanations:

  job-matching-agent.png        (candidate job list ranked by match rating)
  candidate-matching-agent.png  (admin: top matching candidates for a job)
